<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.utsek">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DatadiriDosenActivity"></activity>
        <activity android:name=".DatadiriMahasiswaActivity" />
        <activity android:name=".CrudKrsActivity" />
        <activity android:name=".CrudMatkulActivity" />
        <activity android:name=".CrudMhsActivity" />
        <activity android:name=".recycleviewDosen" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CrudDosenActivity" />
        <activity android:name=".MainDosenActivity" />
        <activity android:name=".MenuDosenActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>